# 第 2 轮：分开裁剪策略与价值网络梯度

父配置：第 1 轮，由此前独立调参验证吞吐量均值选择。唯一主要因素：`training.separate_grad_clip` 从 `False` 改为 `True`。

假设：两个独立网络共用范数上限会产生优化耦合，单独裁剪检验价值误差是否妨碍策略更新；不改变信息、网络或奖励。

所有种子从头训练，各 32,768 步；任务分布、用户真值信息边界和安全约束不变。

## 完整实验结果

训练源提交：`137e17b010fd78270ba0d3885742286f79c02310`。三个种子各 32,768 步，实际设备 MPS。本轮总墙钟耗时 5.43 分钟（含训练和调参评估）。

- **选中的最佳模型：**验证吞吐量 6.434 ± 0.883 Mbps；覆盖率 49.31% ± 6.93 个百分点；每回合拒绝移动 29.58 次。
- **训练结束模型：**验证吞吐量 5.551 ± 0.923 Mbps；覆盖率 52.62% ± 9.95 个百分点；每回合拒绝移动 85.54 次。

误差项为三个训练种子的场景均值之间的样本标准差。所有服务指标采用统一 64 秒窗口。

- 种子 7：选中 23552 步；调参验证最佳 7.052 Mbps，最后 4.912 Mbps。检查点选择集最初四次／最后四次评估均值 5.939／5.532 Mbps。
- 种子 17：选中 13312 步；调参验证最佳 5.422 Mbps，最后 5.131 Mbps。检查点选择集最初四次／最后四次评估均值 5.532／5.476 Mbps。
- 种子 27：选中 17408 步；调参验证最佳 6.827 Mbps，最后 6.609 Mbps。检查点选择集最初四次／最后四次评估均值 6.177／5.529 Mbps。

相同验证场景基线：waypoint 6.232 Mbps / 66.94%；greedy 8.045 Mbps / 70.54%；random 7.558 Mbps / 63.34%；stationary 7.342 Mbps / 61.84%。

[完整配置、逐场景结果、逐更新曲线与来源](r02/results.json)。全部训练回合及更新日志无损保存在同目录 `seed*-metrics.jsonl.gz`；模型在 `output/fixed-window/r02/seed*/`，最佳与最后检查点的 SHA-256 均在结果文件中。最终留出测试尚未用于此轮决策。

## 决策

相对父配置，独立调参验证平均吞吐量变化 +0.231 Mbps；成为后续候选父配置。这里只是调参依据，不是统计显著性或最终测试结论。
