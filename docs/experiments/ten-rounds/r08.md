# 第 8 轮：增加网络隐藏宽度

父配置：第 5 轮，由此前独立调参验证吞吐量均值选择。唯一主要因素：`training.hidden_size` 从 `64` 改为 `128`。

假设：检验当前模型容量是否限制对不同起点和聚类场景的适应；不改变观测信息或任务。

所有种子从头训练，各 32,768 步；任务分布、用户真值信息边界和安全约束不变。

## 完整实验结果

训练源提交：`b0a4b207f0b6c665c14237c65cbb0a9329cc818f`。三个种子各 32,768 步，实际设备 MPS。本轮总墙钟耗时 5.92 分钟（含训练和调参评估）。

- **选中的最佳模型：**验证吞吐量 6.509 ± 0.933 Mbps；覆盖率 56.44% ± 10.71 个百分点；每回合拒绝移动 47.29 次。
- **训练结束模型：**验证吞吐量 5.719 ± 1.235 Mbps；覆盖率 45.70% ± 14.00 个百分点；每回合拒绝移动 87.88 次。

误差项为三个训练种子的场景均值之间的样本标准差。所有服务指标采用统一 64 秒窗口。

- 种子 7：选中 22528 步；调参验证最佳 5.518 Mbps，最后 5.234 Mbps。检查点选择集最初四次／最后四次评估均值 5.780／4.759 Mbps。
- 种子 17：选中 20480 步；调参验证最佳 6.639 Mbps，最后 7.122 Mbps。检查点选择集最初四次／最后四次评估均值 5.491／6.020 Mbps。
- 种子 27：选中 8192 步；调参验证最佳 7.371 Mbps，最后 4.800 Mbps。检查点选择集最初四次／最后四次评估均值 5.780／4.431 Mbps。

相同验证场景基线：waypoint 6.232 Mbps / 66.94%；greedy 8.045 Mbps / 70.54%；random 7.558 Mbps / 63.34%；stationary 7.342 Mbps / 61.84%。

[完整配置、逐场景结果、逐更新曲线与来源](r08/results.json)。全部训练回合及更新日志无损保存在同目录 `seed*-metrics.jsonl.gz`；模型在 `output/fixed-window/r08/seed*/`，最佳与最后检查点的 SHA-256 均在结果文件中。最终留出测试尚未用于此轮决策。

## 决策

相对父配置，独立调参验证平均吞吐量变化 -0.440 Mbps；保留失败/未改善结果，后续回退到此前最优配置。这里只是调参依据，不是统计显著性或最终测试结论。
