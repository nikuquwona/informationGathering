# 第 9 轮：缩短 GP 空间相关长度

父配置：第 5 轮，由此前独立调参验证吞吐量均值选择。唯一主要因素：`environment.gp_length_scale` 从 `15.0` 改为 `7.5`。

假设：原先 15 米相关长度可能平滑掉较窄的人群信号簇；减半检验感知分辨能力，使用同样训练和测试分布。

所有种子从头训练，各 32,768 步；任务分布、用户真值信息边界和安全约束不变。

## 完整实验结果

训练源提交：`fa0d5189a129604e47f7f71e757e97f593caf264`。三个种子各 32,768 步，实际设备 MPS。本轮总墙钟耗时 5.72 分钟（含训练和调参评估）。

- **选中的最佳模型：**验证吞吐量 5.975 ± 0.458 Mbps；覆盖率 49.86% ± 13.60 个百分点；每回合拒绝移动 41.00 次。
- **训练结束模型：**验证吞吐量 6.667 ± 1.129 Mbps；覆盖率 59.20% ± 8.49 个百分点；每回合拒绝移动 55.67 次。

误差项为三个训练种子的场景均值之间的样本标准差。所有服务指标采用统一 64 秒窗口。

- 种子 7：选中 7168 步；调参验证最佳 5.995 Mbps，最后 7.364 Mbps。检查点选择集最初四次／最后四次评估均值 5.933／6.567 Mbps。
- 种子 17：选中 14336 步；调参验证最佳 5.508 Mbps，最后 7.272 Mbps。检查点选择集最初四次／最后四次评估均值 5.358／6.823 Mbps。
- 种子 27：选中 14336 步；调参验证最佳 6.423 Mbps，最后 5.365 Mbps。检查点选择集最初四次／最后四次评估均值 6.186／4.544 Mbps。

相同验证场景基线：waypoint 6.232 Mbps / 66.94%；greedy 6.255 Mbps / 68.56%；random 7.558 Mbps / 63.34%；stationary 7.342 Mbps / 61.84%。

[完整配置、逐场景结果、逐更新曲线与来源](r09/results.json)。全部训练回合及更新日志无损保存在同目录 `seed*-metrics.jsonl.gz`；模型在 `output/fixed-window/r09/seed*/`，最佳与最后检查点的 SHA-256 均在结果文件中。最终留出测试尚未用于此轮决策。

## 决策

相对父配置，独立调参验证平均吞吐量变化 -0.974 Mbps；保留失败/未改善结果，后续回退到此前最优配置。这里只是调参依据，不是统计显著性或最终测试结论。

## 归因边界

`gp_length_scale` 同时影响 GP 核函数、感知重叠降权和贪心基线的目标分散。因此这轮报告的是单个配置参数的联合影响，不是纯 GP 核长度的独立消融；物理地图、用户分布、时间窗口与安全约束没有改变。
