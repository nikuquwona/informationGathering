# 第 9 轮：缩短 GP 空间相关长度

父配置：第 5 轮，由此前独立调参验证吞吐量均值选择。唯一主要因素：`environment.gp_length_scale` 从 `default` 改为 `7.5`。

假设：原先 15 米相关长度可能平滑掉较窄的人群信号簇；减半检验感知分辨能力，使用同样训练和测试分布。

所有种子从头训练，各 32,768 步；任务分布、用户真值信息边界和安全约束不变。
